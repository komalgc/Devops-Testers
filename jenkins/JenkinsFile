pipeline {
    agent any

    stages {
        stage('Unit Tests -Backend') {
            agent {
                docker {
                    image 'golang:1.21-alpine'  // Replace with a stable, supported image
                    reuseNode true
                }
            }
            steps {
                script {
                    dir('bugtracker-backend') {
                        sh 'go test -v ./...'
                    }
                }
            }
        }
    }
}
